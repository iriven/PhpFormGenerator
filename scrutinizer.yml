build:
    nodes:
        analysis:
            project_setup:
                override: true
            tests:
                override:
                    - php-scrutinizer-run --enable-security-analysis
                    
        tests: true
checks:
    php: true
coding_style:
    php:
        indentation:
            general:
                size: 2
filter: {  }
